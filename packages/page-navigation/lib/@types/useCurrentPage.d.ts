import { type Store } from '@react-pdf-viewer/core';
import { type StoreProps } from './types/StoreProps';
export declare const useCurrentPage: (store: Store<StoreProps>) => {
    currentPage: number;
};
