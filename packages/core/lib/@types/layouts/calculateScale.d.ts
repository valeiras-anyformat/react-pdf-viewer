import { SpecialZoomLevel } from '../structs/SpecialZoomLevel';
import { ViewMode } from '../structs/ViewMode';
export declare const calculateScale: (container: HTMLElement, pageHeight: number, pageWidth: number, scale: SpecialZoomLevel, viewMode: ViewMode, numPages: number) => number;
