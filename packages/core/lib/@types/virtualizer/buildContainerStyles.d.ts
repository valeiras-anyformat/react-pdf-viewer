import * as React from 'react';
import { ScrollMode } from '../structs/ScrollMode';
import { type Rect } from '../types/Rect';
export declare const buildContainerStyles: (totalSize: Rect, scrollMode: ScrollMode) => React.CSSProperties;
