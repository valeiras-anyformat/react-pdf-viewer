import { ScrollMode, type Store } from '@react-pdf-viewer/core';
import { type StoreProps } from './types/StoreProps';
export declare const switchScrollMode: (store: Store<StoreProps>, scrollMode: ScrollMode) => void;
